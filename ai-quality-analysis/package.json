{
  "name": "ai-quality-analysis",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small, real AI integration for the Karate API test suite: sends one captured request/response to a real LLM for semantic risk review. Never decides pass/fail — that stays fully deterministic in Karate.",
  "scripts": {
    "analyze": "node analyze-response.js fixtures/sample-response.json"
  },
  "engines": {
    "node": ">=18"
  }
}
